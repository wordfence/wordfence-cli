name: "Unit Testing"
on: [push]
jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update && sudo apt-get install -y python3-mysqldb python3-requests
      - run: python3 -m unittest
